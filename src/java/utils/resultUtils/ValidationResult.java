package utils.resultUtils;

/**
 * ValidationResult is a subclass of BaseResult that is specifically designed
 * to represent the result of validation operations.
 * This class holds a Boolean value indicating whether the validation was successful or not.
 * 
 * @param <Boolean> The type of data returned, which in this case is a Boolean (true or false).
 */
public class ValidationResult extends BaseResult<Boolean> {
    // No additional methods are needed for now since it's directly inheriting from BaseResult
}
